# 企业差旅智能规划 Agent：设计思路

## 一、需求来源

企业采购这类 agent，本质上是在解决一个长期存在但一直没被解好的矛盾：**差旅是可控成本里的大头，但控成本的手段会牺牲员工效率和体验**。传统做法要么管得死（层层审批、指定渠道、预算卡死），要么管不住（员工自己订、事后报销、超标靠人情）。Agent 有机会同时改善这两端。

### 成本侧

- 差旅通常是企业除人力外第二大可控支出，但预订行为极度分散，议价能力被稀释。集中到统一入口才有供应商谈判的筹码。
- 「渠道外预订」是最大的漏损来源。堵不住的原因往往不是员工不守规矩，而是官方系统比消费级 App 难用太多。
- 提前预订天数、退改签损失、协议价利用率这些指标，需要有人在预订那一刻就介入提醒，而不是月底出报表时才发现。

### 合规侧

- 差标规则实际很复杂：职级 × 城市 × 淡旺季 × 是否可向客户转嫁。硬编码会僵化，写进制度文档又没人看。
- 超标申请、特殊情况例外，本来就是「判断题」而非「规则题」。

### 效率侧

- 员工订一次复杂行程平均要花半小时到一小时，跨系统切换、比价、走审批，这段时间产出为零。
- 变更场景更痛：航班延误、会议改期、临时加一站，需要重新协调机酒车、通知同行人、改审批单。

### 数据与后置流程

- 行程、发票、行程单、报销单的匹配，目前大量依赖手工整理和人工核对。
- 碳排放核算（ESG 披露要求）同样依赖完整的行程数据。

### 风险管理

- 出差员工的实时位置和行程，在突发事件时是刚需（duty of care）。

> **需求分布很不均匀。** 咨询、审计、大客户销售、工程施工、外贸等差旅密集行业需求强烈且愿意付费；差旅偶发的中小企业痛感有限。此外，决策者（财务、行政）和使用者（员工）诉求不一致——前者要管控，后者要方便，产品如果只满足一边，要么员工绕过它，要么财务不批预算。

---

## 二、核心设计原则

### 1. 模型负责理解和编排，不负责决策

最容易犯的错，是把差标合规交给 LLM 判断。差标是确定性规则，必须落在规则引擎里，并且作为下单前的**最后一道闸**去校验，而不是靠 prompt 约束模型"不要推荐超标选项"。

模型可以推荐、可以解释规则、可以帮员工把超标理由写清楚，但"这单能不能下"绝不能由一个概率输出来决定。

同理，金额、日期、乘机人、成本中心这些关键字段只能从结构化槽位透传，不允许模型在生成过程中改写。

### 2. 写操作按事务来设计

订票是花钱且不可逆的动作，这是和普通问答类 agent 最大的区别。强制要求：

- **下单前必须有一次结构化确认**——把即将执行的内容以卡片形式摊开让人点，而不是模型说完一段话就去调 API
- **幂等键**，防止超时重试导致重复出票
- **补偿逻辑**：机票出了、酒店没订到，要有明确的回滚或降级路径，不能留半截烂摊子
- 每一步工具调用留完整日志，出问题能追溯到是谁、在哪一步、基于什么信息做的

真出一次重复扣款或者订错人，企业对整个产品的信任就没了。

### 3. 体验的核心是"少问"，而少问靠数据不靠模型

理想交互是「下周二去深圳见张总，当天回」→ 直接出方案。要做到这点，agent 需要拿到：

- 日历（会议时间地点）
- CRM（客户地址）
- 历史行为（偏好航司、常住酒店、座位偏好）
- 组织数据（职级、成本中心、同行同事）

**最大的精力应放在上下文数据层，而不是反复调 prompt。** 没有这层，agent 只是个换了皮的搜索框，员工用两次就回去用携程了。

### 4. 管控做成引导，而不是拦截

解决"财务要管、员工要方便"的矛盾，办法是把成本信息前置到选择那一刻：

- 合规选项排序靠前
- 超标选项直接标注「超出差标 300 元，需 VP 审批」
- 主动给出「晚走两小时省 800」这类可选建议

让员工在做选择时就知道代价，比事后驳回有效得多，也不招恨。

### 5. 事件驱动，不只是问答

差旅的价值高地在**变更**而非首次预订。航班延误、会议改期、临时加一站，这些是外部事件触发的。架构上必须有一条主动通道：监听航变和日程变更，主动推方案给员工。

这是和"能订票的聊天框"真正拉开差距的地方，也是最难被现成产品替代的部分。

### 6. 渐进授权

不要一上来就全自动代订。分四档放权：

1. 只读推荐
2. 生成待确认订单
3. 低风险场景自动执行（打车、改签）
4. 全流程托管

让企业按自己的风险偏好选，也给产品积累准确率数据的时间。

---

## 三、分层架构

| 层 | 性质 | 职责 |
|---|---|---|
| 交互入口 | — | IM、App、日历、邮件 |
| Agent 编排层 | 概率 | 意图理解、槽位补全、方案规划、澄清追问 |
| 策略与事务层 | **确定性** | 差标校验、预算控制、审批路由、幂等、补偿事务 |
| 上下文数据层 | — | 员工偏好、组织架构、协议价、历史行程 |
| 外部集成层 | — | 机酒供应、审批系统、费控、日历、CRM |
| 观测与事件层 | 贯穿 | 决策日志、航变监听、效果评测 |

**关键约束：编排层只能向下提议，不能绕过策略层直接触达集成层。** 这条约束一旦松了，后面所有的审计和责任划分都会塌掉。

---

## 四、最容易被低估的三件事

### 集成才是主要工程量，不是模型

机票走 GDS 还是 OTA、酒店直连还是分销、和企业既有 OA/费控系统对接——这部分占的人力可能是 agent 逻辑本身的三到五倍，而且高度定制化。做产品规划时如果按"模型能力"排期，一定会严重低估。

### 评测不能只看对话质量

北极星指标应定在端到端业务层：

- 渠道内预订率
- 平均预订耗时
- 提前预订天数
- 超标发生率
- 变更场景的人工介入率

模型回答得像模像样但员工还是绕开去用携程，说明产品是失败的。这几个数字比任何 benchmark 都诚实。

### 冷启动要设计

新员工没有偏好数据，新客户没有历史行程。启动阶段要靠组织层面的默认值（同职级同城市的常见选择）过渡，同时把"询问"设计得像正常对话而不是填表。

---

## 五、关于护城河的判断

这类产品真正的护城河不在对话体验，而在**沉淀下来的企业侧数据和策略配置**。用久了之后，agent 对这家公司的差标细则、供应商协议、员工习惯的理解是迁移不走的。

所以早期宁可牺牲一点覆盖广度，也要把单个客户的数据闭环跑通——这比铺很多客户但每家都只做到"能订票"更有价值。
